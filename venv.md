To install `virutalenv`:

```
sudo apt-get install virutalenv
```

To create virutal machine:

```
virtualenv virutalnev-name
```

To activate virutal machine:
```
source venv/bin/activate
```

To install package:

```
pip install package-name
```

